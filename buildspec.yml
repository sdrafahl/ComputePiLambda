version: 0.1
phases:
  install:
    commands:
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket sdrafahl-lambda-pipeline --output-template-file outputSamTemplate.yaml
artifacts:
  type: zip
  file:
    - samTemplate.yaml
    - outputSamTemplate.yaml
